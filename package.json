{
  "name": "record-a-vaccination-guidance",
  "version": "0.0.1",
  "description": "Guidance for the Record a vaccination service.",
  "scripts": {
    "build": "eleventy --quiet",
    "start": "eleventy --serve --quiet"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@x-govuk/nhsuk-eleventy-plugin": "^1.0.5",
    "@nhsbsa/cookie-consent-component": "^0.0.6"
  },
  "private": true,
  "engines": {
    "node": "^22",
    "npm": "^10"
  }
}
